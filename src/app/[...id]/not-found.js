import React from "react";
import styled from "./page.module.css";
const NotFound = () => {
  return (
    <main className={styled.not_found}>
      The file you are looking for does not exist.
    </main>
  );
};

export default NotFound;
